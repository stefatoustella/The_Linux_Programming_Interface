#include "error_functions.h"

int main(void)
{
    /* Smoke test: include the header and ensure the program links.
       We don't call the NORETURN functions here; we only check that
       the header parses correctly in C and links into a trivial app. */
    return 0;
}
